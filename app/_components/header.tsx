import { Card, CardContent } from "./ui/card";

const Header = () => {
  return (
    <Card>
      <CardContent>
        <h1>Header</h1>
      </CardContent>
    </Card>
  );
};

export default Header;
